<h2>Please give a review for <%= @restaurant.name %></h2>

<!-- by passing two objects simpleform knows how to build the action path -->
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>
